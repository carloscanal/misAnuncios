
services:
  # http://localhost:7000
  server:
    build: .
    # environment:
    #   MONGO_URI: mongodb+srv://<USER>:<PASS>@<CLUSTER>.mongodb.net/?retryWrites=true&w=majority
    env_file:
      - .env
    ports:
      - "7000:8000"



  

